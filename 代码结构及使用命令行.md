FinSightAI/
├── api/                    # FastAPI 服务层
│   └── main.py             # API 入口，暴露 /v1/retrieve 和 /v1/chat
├── config/                 # 配置文件目录
├── data/                   # 数据目录
│   ├── raw_pdfs/           # 原始 PDF 文件（54个）
│   ├── processed/          # MinerU 解析后的 Markdown
│   ├── milvus_lite.db      # Milvus Lite 向量数据库
│   └── embeddings/         # 嵌入缓存
├── docs/                   # 文档
├── scripts/                # 可执行脚本
│   ├── clean_em_tags.py    # 数据清洗（去除 em 标签）
│   ├── download_reports.py # 从巨潮资讯网下载公告 PDF
│   ├── sprint1_pipeline.py # Sprint1 主流程（解析+入库+问答）
│   ├── test_sprint2.py     # Sprint2 检索测试
│   └── view_milvus_data.py # 查看 Milvus 数据
├── src/                    # 核心源码
│   ├── data_loader/        # 数据加载模块
│   │   ├── pdf_parser_api.py   # MinerU API PDF 解析器
│   │   ├── chunk_processor.py  # 文本切分处理器
│   │   └── text_splitter.py    # Token 切分器
│   ├── retrieval/          # 检索模块
│   │   ├── milvus_client.py    # Milvus 向量客户端
│   │   ├── bm25_retriever.py   # BM25 稀疏检索
│   │   ├── hybrid_search.py    # 混合检索（RRF 融合+同源去重）
│   │   └── reranker.py         # BGE Reranker 精排
│   ├── services/           # 服务层
│   │   └── rag_service.py      # RAG 服务（检索+问答封装）
│   ├── agents/             # Agent 模块（待开发）
│   ├── perception/         # 感知模块（待开发）
│   ├── storage/            # 存储模块
│   └── tools/              # 工具模块
├── test/                   # 测试文件
├── .env                    # 环境变量（API Key 等）
├── requirements.txt        # Python 依赖
├── clear.py                # 清空 processed 目录
└── README.md               # 项目说明

🚀 可运行脚本及命令
1. FastAPI 服务（主要入口）
功能	            启动命令	               停止命令
启动 API 服务	uvicorn api.main:app --host 0.0.0.0 --port 8000 --reload	Ctrl+C 或 fuser -k 8000/tcp
接口说明：

GET / - 健康检查
POST /v1/retrieve - 四路检索对比（vector/bm25/rrf/rrf_rerank）
POST /v1/chat - RAG 问答

2. 数据处理脚本
脚本	命令	功能	停止
下载公告	python scripts/download_reports.py	从巨潮资讯网爬取 PDF 到 data/raw_pdfs/	Ctrl+C
清洗 em 标签	python scripts/clean_em_tags.py	清洗文件名和内容中的 em 标签	       自动结束
清空 processed	python clear.py	删除 data/processed/ 下所有文件	   自动结束

3. Sprint 流程脚本
脚本	命令	功能	停止
Sprint1 主流程	python scripts/sprint1_pipeline.py	PDF 解析 → 切分 → Milvus 入库 → 交互问答    	Ctrl+C
Sprint2 检索测试	python scripts/test_sprint2.py	测试向量/BM25/RRF/Rerank 四路检索效果	  自动结束
查看 Milvus 数据	python scripts/view_milvus_data.py	查看数据库中的文档数量和样本	  自动结束

4. 测试脚本
脚本	命令	功能
环境测试	 python test/test_env.py	测试环境变量和依赖
Milvus 测试	 python test/test_milvus.py	测试 Milvus 连接
OpenAI 测试	 python test/test_openai.py	测试 SiliconFlow API
混合检索测试	python test/test_hybrid_search.py	测试混合检索功能

🛑 通用停止命令
场景	命令
停止前台进程	Ctrl+C
停止后台进程（按端口）	fuser -k 8000/tcp
停止后台进程（按名称）	pkill -f "uvicorn api.main"
查看占用端口的进程	lsof -i :8000
强制杀死进程	kill -9 <PID>

📋 典型工作流
# 1. 下载公告 PDF
python scripts/download_reports.py

# 2. 清洗 em 标签（如有）
python scripts/clean_em_tags.py

# 3. 解析 PDF 并入库（交互模式可直接问答）
python scripts/sprint1_pipeline.py

# 4. 启动 API 服务
uvicorn api.main:app --host 0.0.0.0 --port 8000 --reload

# 5. 测试 API
curl -X POST http://127.0.0.1:8000/v1/chat \
  -H "Content-Type: application/json" \
  -d '{"messages":[{"role":"user","content":"充电桩建设有哪些公告？"}]}'